name: Ghost Bridge Operational
on:
  workflow_dispatch:

jobs:
  establish-bridge:
    runs-on: ubuntu-latest
    steps:
      - name: Connectivity Test
        run: |
          # تست پینگ (ممکن است توسط دیتاسنتر مسدود باشد)
          ping -c 4 87.107.12.8 || echo "Ping blocked"
